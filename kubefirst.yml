cloudProvider: civo
clusterName: goop-boop
domainName: feedkray.com

dns:
  provider: cloudflare

git:
  auth: https
  github:
    username: jarededwards
    organization: jarededwards
    repo: goop
  # gitlab:
  #   username: jarededwards
  #   # the fully qualified group path in gitlab my/gitlab/group
  #   group: kubefirst-test
  #   repo: goop


# ingresNginx:
#   extraArgs:
#     - --enable-ssl-passthrough
